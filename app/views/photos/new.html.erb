<%= render :partial => 'projects/editnav' %>
<div class="container-fluid">
  <div class="row">
    <div class="col-md-4 col-md-offset-4 text-center">

      <%= bootstrap_form_for([@project, @photo]) do |f| %>
      <%= f.file_field :slide %>
      <%= f.text_field :caption %>
      <%= f.submit %>
      <% end %>
      <%= current_project_photo.title %>
    </div>
  </div>
</div>